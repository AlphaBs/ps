// https://www.acmicpc.net/problem/32870

#include <iostream>
#include <cstdio>
#include <algorithm>

using namespace std;

int arr[333'333];

int main() 
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int n, q;
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    for (int i = 0; i < q; i++)
    {
        int m;
        cin >> m;

        
    }

    return 0;
}